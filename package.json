{
  "name": "@open-paws/mcp-server-nav-language",
  "version": "0.1.0",
  "description": "NAV Language Checker MCP server — real-time speciesist language detection for any MCP-compatible agent",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "node --watch dist/index.js",
    "start": "node dist/index.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.22.0",
    "yaml": "^2.4.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^2.0.0",
    "@types/node": "^22.0.0"
  },
  "engines": { "node": ">=22.0.0" },
  "license": "GPL-3.0-only"
}
